import React from 'react';

// router
import { HashRouter } from 'react-router-dom';

const App: React.FunctionComponent = () => {
    return (
        <HashRouter>
            <div className='App'>
                <div>app</div>
            </div>
        </HashRouter>
    );
};

export default App;
